import { DateCalendarPipe } from './date-calendar.pipe';

describe('DateCalendarPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCalendarPipe();
    expect(pipe).toBeTruthy();
  });
});
