import { CustomerRoutineView } from './customer-routine-view';

describe('CustomerRoutineView', () => {
  it('should create an instance', () => {
    expect(new CustomerRoutineView()).toBeTruthy();
  });
});
